/**
 * Map data registry.
 *
 * Cartography artefacts (TopoJSON of country boundaries, IXP location
 * point sets, cable polylines) live here. The folder is intentionally
 * empty until the corresponding entity records are verified — see
 * /methodology.
 */

export const MAPS_VERSION = "0.0.0" as const;
