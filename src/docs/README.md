# Editorial governance docs

This folder records editorial decisions that can't be derived from
the source registry or the entity schemas alone:

- Trust-tier promotions and demotions (with the editor and the
  reasoning).
- Methodology revisions (one entry per published change).
- Source decommissions (when a source is removed and why).

The folder is empty by design; entries are written when governance
events actually occur.
